fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Same packages the desktop app uses for these two steps, but the headless
# OpenCV build here: scanner_v0_8_dynamic.py never calls imshow/GUI
# functions, so this avoids pulling in GTK/Qt system libs a headless
# NAS/Docker container doesn't have and doesn't need.
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0
anthropic>=0.40
pydantic>=2.0

# Supabase Postgres + Storage client used by db.py/storage.py (via
# supabase_client.py), imported unconditionally at main.py module load.
supabase>=2.0
